module.exports = function check(str, bracketsConfig) {
  // your solution
    if (str.length % 2 !== 0)
        return false;
    if (str.length === 0) {
        return true;

    } else {
        return false;
    }
}
